# Tasks

1.  **實作搖桿輪詢**:
    *   在 `game.js` 中新增一個函式，以在每一幀中輪詢可用的搖桿。
2.  **更新遊戲邏輯**:
    *   修改 `game.logic.js` 中的 `movePaddle` 函式，以使用搖桿輸入。
    *   在 `handleKeyDown` 和 `handleKeyUp` 中新增對搖桿按鈕的支援。
3.  **測試**:
    *   手動測試以確保搖桿控制如預期般運作。
