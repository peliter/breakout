# Tasks

1.  **更新開始畫面**:
    *   在 `game.js` 的 `drawStartScreen` 函式中新增「挑戰模式」按鈕。
    *   在 `handleCanvasClick` 函式中新增處理「挑戰模式」按鈕點擊的邏輯。
2.  **實作關卡設計**:
    *   在 `game.logic.js` 中定義 10 個關卡的方塊排列和強度。
3.  **實作挑戰模式邏輯**:
    *   在 `game.logic.js` 中新增 `startChallengeMode` 函式來初始化挑戰模式。
    *   修改 `update` 函式以處理挑戰模式的遊戲邏輯，包括：
        *   載入目前關卡的方塊。
        *   檢查過關條件。
        *   處理方塊向下移動和左右晃動。
4.  **顯示關卡資訊**:
    *   在 `game.js` 的 `draw` 函式中顯示目前的關卡編號。
5.  **測試**:
    *   手動測試以確保挑戰模式如預期般運作。
