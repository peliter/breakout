## 1. 視覺與音效資源
- [ ] 1.1 尋找或建立用於球和擋板的 placeholder 圖片資源。
- [ ] 1.2 尋找或建立用於關鍵事件的 placeholder 音效檔案（例如：`paddle_hit.wav`, `wall_bounce.wav`）。
- [ ] 1.3 將資源檔案整合到專案中。

## 2. 功能：動態速度
- [x] 2.1 在 `game.logic.js` 中，建立一個函式，根據目前分數計算球的新速度。
- [x] x] 2.2 在遊戲主循環中，每次分數更新時呼叫此函式來調整球速。
- [x] 2.3 實作一個在速度提升時，於畫面上顯示非侵入式提示的函式 (例如 "Speed Up!")。
- [x] 2.4 撰寫測試以驗證速度是否如預期般隨著分數增加。

## 3. 功能：視覺強化
- [x] 3.1 在 `game.js` 中，修改渲染邏輯，使用圖片來繪製球和擋板，取代原來的 `fillRect`。 (目前使用繪圖原語實作拖尾效果，圖片待補充)
- [x] 3.2 實作球和擋板的拖尾效果。可以在每一幀繪製多個半透明的影像，或者使用粒子系統來達成。
- [x] 3.3 確保新視覺效果的性能流暢。

## 4. 功能：增加音效
- [x] 4.1 在 `game.js` 或一個新的模組中，建立一個音效管理器，用於載入和播放音效。
- [x] 4.2 在 `game.logic.js` 的碰撞檢測邏輯中，新增事件觸發機制，當發生碰撞時發出音效事件。
- [x] 4.3 在 `game.js` 中監聽這些事件，並呼叫音效管理器播放對應的音效。

## 5. 整合與測試
- [ ] 5.1 進行完整的遊戲測試，確保所有新功能都正常運作且彼此沒有衝突。
- [ ] 5.2 調整遊戲平衡性，包括速度增加的曲線和音效的音量。