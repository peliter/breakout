## 為什麼
使用者想要為專案新增一個打磚塊遊戲功能，並從一個只包含擋板和球的簡化版本開始。

## 變更內容
- 引入一個新的「打磚塊遊戲」模組。
- 實現核心遊戲機制（擋板、球、碰撞偵測）。
- 每次球碰到擋板，分數加 1。
- 實現遊戲狀態（開始、遊戲中、遊戲結束）。

## 影響
- 受影響的規格：將新增一個新的 `game` 能力。
- 受影響的程式碼：新的遊戲相關檔案，並可能需要修改主應用程式的進入點以整合遊戲。
